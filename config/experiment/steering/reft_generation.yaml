defaults:
  - /model/tinyllama_gemma2b_reft
  - /dataset/concept500_2b_l10
  - /training/base
  - /loss/base
  - /wandb_config/base
  - _self_

training:
  n_epochs: 5
  eval_per_steps: 100
  save_model: true
  debug_model: true
  train_batch_size: 2
  test_batch_size: 16

model:
  max_length: 128
  das_dimension: 1

loss:
  return_penalty: false

wandb_config:
  project: HyperReFT_Steering
  run_name: llama38b_gemma2b_hyperloreft_steer